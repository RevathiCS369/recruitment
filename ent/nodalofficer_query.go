// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"recruit/ent/center"
	"recruit/ent/exam"
	"recruit/ent/nodalofficer"
	"recruit/ent/notification"
	"recruit/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// NodalOfficerQuery is the builder for querying NodalOfficer entities.
type NodalOfficerQuery struct {
	config
	ctx              *QueryContext
	order            []nodalofficer.OrderOption
	inters           []Interceptor
	predicates       []predicate.NodalOfficer
	withCenters      *CenterQuery
	withExam         *ExamQuery
	withNotification *NotificationQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NodalOfficerQuery builder.
func (noq *NodalOfficerQuery) Where(ps ...predicate.NodalOfficer) *NodalOfficerQuery {
	noq.predicates = append(noq.predicates, ps...)
	return noq
}

// Limit the number of records to be returned by this query.
func (noq *NodalOfficerQuery) Limit(limit int) *NodalOfficerQuery {
	noq.ctx.Limit = &limit
	return noq
}

// Offset to start from.
func (noq *NodalOfficerQuery) Offset(offset int) *NodalOfficerQuery {
	noq.ctx.Offset = &offset
	return noq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (noq *NodalOfficerQuery) Unique(unique bool) *NodalOfficerQuery {
	noq.ctx.Unique = &unique
	return noq
}

// Order specifies how the records should be ordered.
func (noq *NodalOfficerQuery) Order(o ...nodalofficer.OrderOption) *NodalOfficerQuery {
	noq.order = append(noq.order, o...)
	return noq
}

// QueryCenters chains the current query on the "centers" edge.
func (noq *NodalOfficerQuery) QueryCenters() *CenterQuery {
	query := (&CenterClient{config: noq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := noq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := noq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(nodalofficer.Table, nodalofficer.FieldID, selector),
			sqlgraph.To(center.Table, center.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, nodalofficer.CentersTable, nodalofficer.CentersColumn),
		)
		fromU = sqlgraph.SetNeighbors(noq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryExam chains the current query on the "exam" edge.
func (noq *NodalOfficerQuery) QueryExam() *ExamQuery {
	query := (&ExamClient{config: noq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := noq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := noq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(nodalofficer.Table, nodalofficer.FieldID, selector),
			sqlgraph.To(exam.Table, exam.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, nodalofficer.ExamTable, nodalofficer.ExamColumn),
		)
		fromU = sqlgraph.SetNeighbors(noq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNotification chains the current query on the "notification" edge.
func (noq *NodalOfficerQuery) QueryNotification() *NotificationQuery {
	query := (&NotificationClient{config: noq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := noq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := noq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(nodalofficer.Table, nodalofficer.FieldID, selector),
			sqlgraph.To(notification.Table, notification.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, nodalofficer.NotificationTable, nodalofficer.NotificationColumn),
		)
		fromU = sqlgraph.SetNeighbors(noq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first NodalOfficer entity from the query.
// Returns a *NotFoundError when no NodalOfficer was found.
func (noq *NodalOfficerQuery) First(ctx context.Context) (*NodalOfficer, error) {
	nodes, err := noq.Limit(1).All(setContextOp(ctx, noq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{nodalofficer.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (noq *NodalOfficerQuery) FirstX(ctx context.Context) *NodalOfficer {
	node, err := noq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NodalOfficer ID from the query.
// Returns a *NotFoundError when no NodalOfficer ID was found.
func (noq *NodalOfficerQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = noq.Limit(1).IDs(setContextOp(ctx, noq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{nodalofficer.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (noq *NodalOfficerQuery) FirstIDX(ctx context.Context) int32 {
	id, err := noq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NodalOfficer entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NodalOfficer entity is found.
// Returns a *NotFoundError when no NodalOfficer entities are found.
func (noq *NodalOfficerQuery) Only(ctx context.Context) (*NodalOfficer, error) {
	nodes, err := noq.Limit(2).All(setContextOp(ctx, noq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{nodalofficer.Label}
	default:
		return nil, &NotSingularError{nodalofficer.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (noq *NodalOfficerQuery) OnlyX(ctx context.Context) *NodalOfficer {
	node, err := noq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NodalOfficer ID in the query.
// Returns a *NotSingularError when more than one NodalOfficer ID is found.
// Returns a *NotFoundError when no entities are found.
func (noq *NodalOfficerQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = noq.Limit(2).IDs(setContextOp(ctx, noq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{nodalofficer.Label}
	default:
		err = &NotSingularError{nodalofficer.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (noq *NodalOfficerQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := noq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NodalOfficers.
func (noq *NodalOfficerQuery) All(ctx context.Context) ([]*NodalOfficer, error) {
	ctx = setContextOp(ctx, noq.ctx, "All")
	if err := noq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NodalOfficer, *NodalOfficerQuery]()
	return withInterceptors[[]*NodalOfficer](ctx, noq, qr, noq.inters)
}

// AllX is like All, but panics if an error occurs.
func (noq *NodalOfficerQuery) AllX(ctx context.Context) []*NodalOfficer {
	nodes, err := noq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NodalOfficer IDs.
func (noq *NodalOfficerQuery) IDs(ctx context.Context) (ids []int32, err error) {
	if noq.ctx.Unique == nil && noq.path != nil {
		noq.Unique(true)
	}
	ctx = setContextOp(ctx, noq.ctx, "IDs")
	if err = noq.Select(nodalofficer.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (noq *NodalOfficerQuery) IDsX(ctx context.Context) []int32 {
	ids, err := noq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (noq *NodalOfficerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, noq.ctx, "Count")
	if err := noq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, noq, querierCount[*NodalOfficerQuery](), noq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (noq *NodalOfficerQuery) CountX(ctx context.Context) int {
	count, err := noq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (noq *NodalOfficerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, noq.ctx, "Exist")
	switch _, err := noq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (noq *NodalOfficerQuery) ExistX(ctx context.Context) bool {
	exist, err := noq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NodalOfficerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (noq *NodalOfficerQuery) Clone() *NodalOfficerQuery {
	if noq == nil {
		return nil
	}
	return &NodalOfficerQuery{
		config:           noq.config,
		ctx:              noq.ctx.Clone(),
		order:            append([]nodalofficer.OrderOption{}, noq.order...),
		inters:           append([]Interceptor{}, noq.inters...),
		predicates:       append([]predicate.NodalOfficer{}, noq.predicates...),
		withCenters:      noq.withCenters.Clone(),
		withExam:         noq.withExam.Clone(),
		withNotification: noq.withNotification.Clone(),
		// clone intermediate query.
		sql:  noq.sql.Clone(),
		path: noq.path,
	}
}

// WithCenters tells the query-builder to eager-load the nodes that are connected to
// the "centers" edge. The optional arguments are used to configure the query builder of the edge.
func (noq *NodalOfficerQuery) WithCenters(opts ...func(*CenterQuery)) *NodalOfficerQuery {
	query := (&CenterClient{config: noq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	noq.withCenters = query
	return noq
}

// WithExam tells the query-builder to eager-load the nodes that are connected to
// the "exam" edge. The optional arguments are used to configure the query builder of the edge.
func (noq *NodalOfficerQuery) WithExam(opts ...func(*ExamQuery)) *NodalOfficerQuery {
	query := (&ExamClient{config: noq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	noq.withExam = query
	return noq
}

// WithNotification tells the query-builder to eager-load the nodes that are connected to
// the "notification" edge. The optional arguments are used to configure the query builder of the edge.
func (noq *NodalOfficerQuery) WithNotification(opts ...func(*NotificationQuery)) *NodalOfficerQuery {
	query := (&NotificationClient{config: noq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	noq.withNotification = query
	return noq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		NodalOfficerName string `json:"NodalOfficerName,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NodalOfficer.Query().
//		GroupBy(nodalofficer.FieldNodalOfficerName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (noq *NodalOfficerQuery) GroupBy(field string, fields ...string) *NodalOfficerGroupBy {
	noq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NodalOfficerGroupBy{build: noq}
	grbuild.flds = &noq.ctx.Fields
	grbuild.label = nodalofficer.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		NodalOfficerName string `json:"NodalOfficerName,omitempty"`
//	}
//
//	client.NodalOfficer.Query().
//		Select(nodalofficer.FieldNodalOfficerName).
//		Scan(ctx, &v)
func (noq *NodalOfficerQuery) Select(fields ...string) *NodalOfficerSelect {
	noq.ctx.Fields = append(noq.ctx.Fields, fields...)
	sbuild := &NodalOfficerSelect{NodalOfficerQuery: noq}
	sbuild.label = nodalofficer.Label
	sbuild.flds, sbuild.scan = &noq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NodalOfficerSelect configured with the given aggregations.
func (noq *NodalOfficerQuery) Aggregate(fns ...AggregateFunc) *NodalOfficerSelect {
	return noq.Select().Aggregate(fns...)
}

func (noq *NodalOfficerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range noq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, noq); err != nil {
				return err
			}
		}
	}
	for _, f := range noq.ctx.Fields {
		if !nodalofficer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if noq.path != nil {
		prev, err := noq.path(ctx)
		if err != nil {
			return err
		}
		noq.sql = prev
	}
	return nil
}

func (noq *NodalOfficerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NodalOfficer, error) {
	var (
		nodes       = []*NodalOfficer{}
		_spec       = noq.querySpec()
		loadedTypes = [3]bool{
			noq.withCenters != nil,
			noq.withExam != nil,
			noq.withNotification != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NodalOfficer).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NodalOfficer{config: noq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, noq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := noq.withCenters; query != nil {
		if err := noq.loadCenters(ctx, query, nodes,
			func(n *NodalOfficer) { n.Edges.Centers = []*Center{} },
			func(n *NodalOfficer, e *Center) { n.Edges.Centers = append(n.Edges.Centers, e) }); err != nil {
			return nil, err
		}
	}
	if query := noq.withExam; query != nil {
		if err := noq.loadExam(ctx, query, nodes, nil,
			func(n *NodalOfficer, e *Exam) { n.Edges.Exam = e }); err != nil {
			return nil, err
		}
	}
	if query := noq.withNotification; query != nil {
		if err := noq.loadNotification(ctx, query, nodes, nil,
			func(n *NodalOfficer, e *Notification) { n.Edges.Notification = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (noq *NodalOfficerQuery) loadCenters(ctx context.Context, query *CenterQuery, nodes []*NodalOfficer, init func(*NodalOfficer), assign func(*NodalOfficer, *Center)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*NodalOfficer)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(center.FieldNodalOfficerCode)
	}
	query.Where(predicate.Center(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(nodalofficer.CentersColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.NodalOfficerCode
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "NodalOfficerCode" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (noq *NodalOfficerQuery) loadExam(ctx context.Context, query *ExamQuery, nodes []*NodalOfficer, init func(*NodalOfficer), assign func(*NodalOfficer, *Exam)) error {
	ids := make([]int32, 0, len(nodes))
	nodeids := make(map[int32][]*NodalOfficer)
	for i := range nodes {
		fk := nodes[i].ExamCode
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(exam.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "ExamCode" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (noq *NodalOfficerQuery) loadNotification(ctx context.Context, query *NotificationQuery, nodes []*NodalOfficer, init func(*NodalOfficer), assign func(*NodalOfficer, *Notification)) error {
	ids := make([]int32, 0, len(nodes))
	nodeids := make(map[int32][]*NodalOfficer)
	for i := range nodes {
		fk := nodes[i].NotifyCode
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(notification.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "NotifyCode" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (noq *NodalOfficerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := noq.querySpec()
	_spec.Node.Columns = noq.ctx.Fields
	if len(noq.ctx.Fields) > 0 {
		_spec.Unique = noq.ctx.Unique != nil && *noq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, noq.driver, _spec)
}

func (noq *NodalOfficerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(nodalofficer.Table, nodalofficer.Columns, sqlgraph.NewFieldSpec(nodalofficer.FieldID, field.TypeInt32))
	_spec.From = noq.sql
	if unique := noq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if noq.path != nil {
		_spec.Unique = true
	}
	if fields := noq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, nodalofficer.FieldID)
		for i := range fields {
			if fields[i] != nodalofficer.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if noq.withExam != nil {
			_spec.Node.AddColumnOnce(nodalofficer.FieldExamCode)
		}
		if noq.withNotification != nil {
			_spec.Node.AddColumnOnce(nodalofficer.FieldNotifyCode)
		}
	}
	if ps := noq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := noq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := noq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := noq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (noq *NodalOfficerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(noq.driver.Dialect())
	t1 := builder.Table(nodalofficer.Table)
	columns := noq.ctx.Fields
	if len(columns) == 0 {
		columns = nodalofficer.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if noq.sql != nil {
		selector = noq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if noq.ctx.Unique != nil && *noq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range noq.predicates {
		p(selector)
	}
	for _, p := range noq.order {
		p(selector)
	}
	if offset := noq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := noq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// NodalOfficerGroupBy is the group-by builder for NodalOfficer entities.
type NodalOfficerGroupBy struct {
	selector
	build *NodalOfficerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (nogb *NodalOfficerGroupBy) Aggregate(fns ...AggregateFunc) *NodalOfficerGroupBy {
	nogb.fns = append(nogb.fns, fns...)
	return nogb
}

// Scan applies the selector query and scans the result into the given value.
func (nogb *NodalOfficerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, nogb.build.ctx, "GroupBy")
	if err := nogb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NodalOfficerQuery, *NodalOfficerGroupBy](ctx, nogb.build, nogb, nogb.build.inters, v)
}

func (nogb *NodalOfficerGroupBy) sqlScan(ctx context.Context, root *NodalOfficerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(nogb.fns))
	for _, fn := range nogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*nogb.flds)+len(nogb.fns))
		for _, f := range *nogb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*nogb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nogb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NodalOfficerSelect is the builder for selecting fields of NodalOfficer entities.
type NodalOfficerSelect struct {
	*NodalOfficerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (nos *NodalOfficerSelect) Aggregate(fns ...AggregateFunc) *NodalOfficerSelect {
	nos.fns = append(nos.fns, fns...)
	return nos
}

// Scan applies the selector query and scans the result into the given value.
func (nos *NodalOfficerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, nos.ctx, "Select")
	if err := nos.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NodalOfficerQuery, *NodalOfficerSelect](ctx, nos.NodalOfficerQuery, nos, nos.inters, v)
}

func (nos *NodalOfficerSelect) sqlScan(ctx context.Context, root *NodalOfficerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(nos.fns))
	for _, fn := range nos.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*nos.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nos.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
