// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"recruit/ent/exam_applications_ps"
	"recruit/ent/exam_pa"
	"recruit/ent/examcalendar"
	"recruit/ent/exampapers"
	"recruit/ent/notification"
	"recruit/ent/predicate"
	"recruit/ent/usermaster"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ExamPAQuery is the builder for querying Exam_PA entities.
type ExamPAQuery struct {
	config
	ctx                 *QueryContext
	order               []exam_pa.OrderOption
	inters              []Interceptor
	predicates          []predicate.Exam_PA
	withExamcalPsRef    *ExamCalendarQuery
	withPapersPsRef     *ExamPapersQuery
	withUsersPsType     *UserMasterQuery
	withExamApplnPSRef  *ExamApplicationsPSQuery
	withNotificationsPs *NotificationQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ExamPAQuery builder.
func (epq *ExamPAQuery) Where(ps ...predicate.Exam_PA) *ExamPAQuery {
	epq.predicates = append(epq.predicates, ps...)
	return epq
}

// Limit the number of records to be returned by this query.
func (epq *ExamPAQuery) Limit(limit int) *ExamPAQuery {
	epq.ctx.Limit = &limit
	return epq
}

// Offset to start from.
func (epq *ExamPAQuery) Offset(offset int) *ExamPAQuery {
	epq.ctx.Offset = &offset
	return epq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (epq *ExamPAQuery) Unique(unique bool) *ExamPAQuery {
	epq.ctx.Unique = &unique
	return epq
}

// Order specifies how the records should be ordered.
func (epq *ExamPAQuery) Order(o ...exam_pa.OrderOption) *ExamPAQuery {
	epq.order = append(epq.order, o...)
	return epq
}

// QueryExamcalPsRef chains the current query on the "examcal_ps_ref" edge.
func (epq *ExamPAQuery) QueryExamcalPsRef() *ExamCalendarQuery {
	query := (&ExamCalendarClient{config: epq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := epq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := epq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(exam_pa.Table, exam_pa.FieldID, selector),
			sqlgraph.To(examcalendar.Table, examcalendar.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, exam_pa.ExamcalPsRefTable, exam_pa.ExamcalPsRefColumn),
		)
		fromU = sqlgraph.SetNeighbors(epq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPapersPsRef chains the current query on the "papers_ps_ref" edge.
func (epq *ExamPAQuery) QueryPapersPsRef() *ExamPapersQuery {
	query := (&ExamPapersClient{config: epq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := epq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := epq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(exam_pa.Table, exam_pa.FieldID, selector),
			sqlgraph.To(exampapers.Table, exampapers.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, exam_pa.PapersPsRefTable, exam_pa.PapersPsRefColumn),
		)
		fromU = sqlgraph.SetNeighbors(epq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUsersPsType chains the current query on the "users_ps_type" edge.
func (epq *ExamPAQuery) QueryUsersPsType() *UserMasterQuery {
	query := (&UserMasterClient{config: epq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := epq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := epq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(exam_pa.Table, exam_pa.FieldID, selector),
			sqlgraph.To(usermaster.Table, usermaster.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, exam_pa.UsersPsTypeTable, exam_pa.UsersPsTypeColumn),
		)
		fromU = sqlgraph.SetNeighbors(epq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryExamApplnPSRef chains the current query on the "ExamAppln_PS_Ref" edge.
func (epq *ExamPAQuery) QueryExamApplnPSRef() *ExamApplicationsPSQuery {
	query := (&ExamApplicationsPSClient{config: epq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := epq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := epq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(exam_pa.Table, exam_pa.FieldID, selector),
			sqlgraph.To(exam_applications_ps.Table, exam_applications_ps.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, exam_pa.ExamApplnPSRefTable, exam_pa.ExamApplnPSRefColumn),
		)
		fromU = sqlgraph.SetNeighbors(epq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNotificationsPs chains the current query on the "notifications_ps" edge.
func (epq *ExamPAQuery) QueryNotificationsPs() *NotificationQuery {
	query := (&NotificationClient{config: epq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := epq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := epq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(exam_pa.Table, exam_pa.FieldID, selector),
			sqlgraph.To(notification.Table, notification.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, exam_pa.NotificationsPsTable, exam_pa.NotificationsPsColumn),
		)
		fromU = sqlgraph.SetNeighbors(epq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Exam_PA entity from the query.
// Returns a *NotFoundError when no Exam_PA was found.
func (epq *ExamPAQuery) First(ctx context.Context) (*Exam_PA, error) {
	nodes, err := epq.Limit(1).All(setContextOp(ctx, epq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{exam_pa.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (epq *ExamPAQuery) FirstX(ctx context.Context) *Exam_PA {
	node, err := epq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Exam_PA ID from the query.
// Returns a *NotFoundError when no Exam_PA ID was found.
func (epq *ExamPAQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = epq.Limit(1).IDs(setContextOp(ctx, epq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{exam_pa.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (epq *ExamPAQuery) FirstIDX(ctx context.Context) int32 {
	id, err := epq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Exam_PA entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Exam_PA entity is found.
// Returns a *NotFoundError when no Exam_PA entities are found.
func (epq *ExamPAQuery) Only(ctx context.Context) (*Exam_PA, error) {
	nodes, err := epq.Limit(2).All(setContextOp(ctx, epq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{exam_pa.Label}
	default:
		return nil, &NotSingularError{exam_pa.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (epq *ExamPAQuery) OnlyX(ctx context.Context) *Exam_PA {
	node, err := epq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Exam_PA ID in the query.
// Returns a *NotSingularError when more than one Exam_PA ID is found.
// Returns a *NotFoundError when no entities are found.
func (epq *ExamPAQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = epq.Limit(2).IDs(setContextOp(ctx, epq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{exam_pa.Label}
	default:
		err = &NotSingularError{exam_pa.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (epq *ExamPAQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := epq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Exam_PAs.
func (epq *ExamPAQuery) All(ctx context.Context) ([]*Exam_PA, error) {
	ctx = setContextOp(ctx, epq.ctx, "All")
	if err := epq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Exam_PA, *ExamPAQuery]()
	return withInterceptors[[]*Exam_PA](ctx, epq, qr, epq.inters)
}

// AllX is like All, but panics if an error occurs.
func (epq *ExamPAQuery) AllX(ctx context.Context) []*Exam_PA {
	nodes, err := epq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Exam_PA IDs.
func (epq *ExamPAQuery) IDs(ctx context.Context) (ids []int32, err error) {
	if epq.ctx.Unique == nil && epq.path != nil {
		epq.Unique(true)
	}
	ctx = setContextOp(ctx, epq.ctx, "IDs")
	if err = epq.Select(exam_pa.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (epq *ExamPAQuery) IDsX(ctx context.Context) []int32 {
	ids, err := epq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (epq *ExamPAQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, epq.ctx, "Count")
	if err := epq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, epq, querierCount[*ExamPAQuery](), epq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (epq *ExamPAQuery) CountX(ctx context.Context) int {
	count, err := epq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (epq *ExamPAQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, epq.ctx, "Exist")
	switch _, err := epq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (epq *ExamPAQuery) ExistX(ctx context.Context) bool {
	exist, err := epq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ExamPAQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (epq *ExamPAQuery) Clone() *ExamPAQuery {
	if epq == nil {
		return nil
	}
	return &ExamPAQuery{
		config:              epq.config,
		ctx:                 epq.ctx.Clone(),
		order:               append([]exam_pa.OrderOption{}, epq.order...),
		inters:              append([]Interceptor{}, epq.inters...),
		predicates:          append([]predicate.Exam_PA{}, epq.predicates...),
		withExamcalPsRef:    epq.withExamcalPsRef.Clone(),
		withPapersPsRef:     epq.withPapersPsRef.Clone(),
		withUsersPsType:     epq.withUsersPsType.Clone(),
		withExamApplnPSRef:  epq.withExamApplnPSRef.Clone(),
		withNotificationsPs: epq.withNotificationsPs.Clone(),
		// clone intermediate query.
		sql:  epq.sql.Clone(),
		path: epq.path,
	}
}

// WithExamcalPsRef tells the query-builder to eager-load the nodes that are connected to
// the "examcal_ps_ref" edge. The optional arguments are used to configure the query builder of the edge.
func (epq *ExamPAQuery) WithExamcalPsRef(opts ...func(*ExamCalendarQuery)) *ExamPAQuery {
	query := (&ExamCalendarClient{config: epq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	epq.withExamcalPsRef = query
	return epq
}

// WithPapersPsRef tells the query-builder to eager-load the nodes that are connected to
// the "papers_ps_ref" edge. The optional arguments are used to configure the query builder of the edge.
func (epq *ExamPAQuery) WithPapersPsRef(opts ...func(*ExamPapersQuery)) *ExamPAQuery {
	query := (&ExamPapersClient{config: epq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	epq.withPapersPsRef = query
	return epq
}

// WithUsersPsType tells the query-builder to eager-load the nodes that are connected to
// the "users_ps_type" edge. The optional arguments are used to configure the query builder of the edge.
func (epq *ExamPAQuery) WithUsersPsType(opts ...func(*UserMasterQuery)) *ExamPAQuery {
	query := (&UserMasterClient{config: epq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	epq.withUsersPsType = query
	return epq
}

// WithExamApplnPSRef tells the query-builder to eager-load the nodes that are connected to
// the "ExamAppln_PS_Ref" edge. The optional arguments are used to configure the query builder of the edge.
func (epq *ExamPAQuery) WithExamApplnPSRef(opts ...func(*ExamApplicationsPSQuery)) *ExamPAQuery {
	query := (&ExamApplicationsPSClient{config: epq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	epq.withExamApplnPSRef = query
	return epq
}

// WithNotificationsPs tells the query-builder to eager-load the nodes that are connected to
// the "notifications_ps" edge. The optional arguments are used to configure the query builder of the edge.
func (epq *ExamPAQuery) WithNotificationsPs(opts ...func(*NotificationQuery)) *ExamPAQuery {
	query := (&NotificationClient{config: epq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	epq.withNotificationsPs = query
	return epq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ExamNameCode string `json:"ExamNameCode,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ExamPA.Query().
//		GroupBy(exam_pa.FieldExamNameCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (epq *ExamPAQuery) GroupBy(field string, fields ...string) *ExamPAGroupBy {
	epq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ExamPAGroupBy{build: epq}
	grbuild.flds = &epq.ctx.Fields
	grbuild.label = exam_pa.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ExamNameCode string `json:"ExamNameCode,omitempty"`
//	}
//
//	client.ExamPA.Query().
//		Select(exam_pa.FieldExamNameCode).
//		Scan(ctx, &v)
func (epq *ExamPAQuery) Select(fields ...string) *ExamPASelect {
	epq.ctx.Fields = append(epq.ctx.Fields, fields...)
	sbuild := &ExamPASelect{ExamPAQuery: epq}
	sbuild.label = exam_pa.Label
	sbuild.flds, sbuild.scan = &epq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ExamPASelect configured with the given aggregations.
func (epq *ExamPAQuery) Aggregate(fns ...AggregateFunc) *ExamPASelect {
	return epq.Select().Aggregate(fns...)
}

func (epq *ExamPAQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range epq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, epq); err != nil {
				return err
			}
		}
	}
	for _, f := range epq.ctx.Fields {
		if !exam_pa.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if epq.path != nil {
		prev, err := epq.path(ctx)
		if err != nil {
			return err
		}
		epq.sql = prev
	}
	return nil
}

func (epq *ExamPAQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Exam_PA, error) {
	var (
		nodes       = []*Exam_PA{}
		_spec       = epq.querySpec()
		loadedTypes = [5]bool{
			epq.withExamcalPsRef != nil,
			epq.withPapersPsRef != nil,
			epq.withUsersPsType != nil,
			epq.withExamApplnPSRef != nil,
			epq.withNotificationsPs != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Exam_PA).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Exam_PA{config: epq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, epq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := epq.withExamcalPsRef; query != nil {
		if err := epq.loadExamcalPsRef(ctx, query, nodes,
			func(n *Exam_PA) { n.Edges.ExamcalPsRef = []*ExamCalendar{} },
			func(n *Exam_PA, e *ExamCalendar) { n.Edges.ExamcalPsRef = append(n.Edges.ExamcalPsRef, e) }); err != nil {
			return nil, err
		}
	}
	if query := epq.withPapersPsRef; query != nil {
		if err := epq.loadPapersPsRef(ctx, query, nodes,
			func(n *Exam_PA) { n.Edges.PapersPsRef = []*ExamPapers{} },
			func(n *Exam_PA, e *ExamPapers) { n.Edges.PapersPsRef = append(n.Edges.PapersPsRef, e) }); err != nil {
			return nil, err
		}
	}
	if query := epq.withUsersPsType; query != nil {
		if err := epq.loadUsersPsType(ctx, query, nodes,
			func(n *Exam_PA) { n.Edges.UsersPsType = []*UserMaster{} },
			func(n *Exam_PA, e *UserMaster) { n.Edges.UsersPsType = append(n.Edges.UsersPsType, e) }); err != nil {
			return nil, err
		}
	}
	if query := epq.withExamApplnPSRef; query != nil {
		if err := epq.loadExamApplnPSRef(ctx, query, nodes,
			func(n *Exam_PA) { n.Edges.ExamApplnPSRef = []*Exam_Applications_PS{} },
			func(n *Exam_PA, e *Exam_Applications_PS) { n.Edges.ExamApplnPSRef = append(n.Edges.ExamApplnPSRef, e) }); err != nil {
			return nil, err
		}
	}
	if query := epq.withNotificationsPs; query != nil {
		if err := epq.loadNotificationsPs(ctx, query, nodes,
			func(n *Exam_PA) { n.Edges.NotificationsPs = []*Notification{} },
			func(n *Exam_PA, e *Notification) { n.Edges.NotificationsPs = append(n.Edges.NotificationsPs, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (epq *ExamPAQuery) loadExamcalPsRef(ctx context.Context, query *ExamCalendarQuery, nodes []*Exam_PA, init func(*Exam_PA), assign func(*Exam_PA, *ExamCalendar)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*Exam_PA)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.ExamCalendar(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(exam_pa.ExamcalPsRefColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.exam_pa_examcal_ps_ref
		if fk == nil {
			return fmt.Errorf(`foreign-key "exam_pa_examcal_ps_ref" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "exam_pa_examcal_ps_ref" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (epq *ExamPAQuery) loadPapersPsRef(ctx context.Context, query *ExamPapersQuery, nodes []*Exam_PA, init func(*Exam_PA), assign func(*Exam_PA, *ExamPapers)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*Exam_PA)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.ExamPapers(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(exam_pa.PapersPsRefColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.exam_pa_papers_ps_ref
		if fk == nil {
			return fmt.Errorf(`foreign-key "exam_pa_papers_ps_ref" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "exam_pa_papers_ps_ref" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (epq *ExamPAQuery) loadUsersPsType(ctx context.Context, query *UserMasterQuery, nodes []*Exam_PA, init func(*Exam_PA), assign func(*Exam_PA, *UserMaster)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*Exam_PA)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.UserMaster(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(exam_pa.UsersPsTypeColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.exam_pa_users_ps_type
		if fk == nil {
			return fmt.Errorf(`foreign-key "exam_pa_users_ps_type" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "exam_pa_users_ps_type" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (epq *ExamPAQuery) loadExamApplnPSRef(ctx context.Context, query *ExamApplicationsPSQuery, nodes []*Exam_PA, init func(*Exam_PA), assign func(*Exam_PA, *Exam_Applications_PS)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*Exam_PA)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Exam_Applications_PS(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(exam_pa.ExamApplnPSRefColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.exam_pa_exam_appln_ps_ref
		if fk == nil {
			return fmt.Errorf(`foreign-key "exam_pa_exam_appln_ps_ref" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "exam_pa_exam_appln_ps_ref" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (epq *ExamPAQuery) loadNotificationsPs(ctx context.Context, query *NotificationQuery, nodes []*Exam_PA, init func(*Exam_PA), assign func(*Exam_PA, *Notification)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*Exam_PA)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Notification(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(exam_pa.NotificationsPsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.exam_pa_notifications_ps
		if fk == nil {
			return fmt.Errorf(`foreign-key "exam_pa_notifications_ps" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "exam_pa_notifications_ps" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (epq *ExamPAQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := epq.querySpec()
	_spec.Node.Columns = epq.ctx.Fields
	if len(epq.ctx.Fields) > 0 {
		_spec.Unique = epq.ctx.Unique != nil && *epq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, epq.driver, _spec)
}

func (epq *ExamPAQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(exam_pa.Table, exam_pa.Columns, sqlgraph.NewFieldSpec(exam_pa.FieldID, field.TypeInt32))
	_spec.From = epq.sql
	if unique := epq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if epq.path != nil {
		_spec.Unique = true
	}
	if fields := epq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, exam_pa.FieldID)
		for i := range fields {
			if fields[i] != exam_pa.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := epq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := epq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := epq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := epq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (epq *ExamPAQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(epq.driver.Dialect())
	t1 := builder.Table(exam_pa.Table)
	columns := epq.ctx.Fields
	if len(columns) == 0 {
		columns = exam_pa.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if epq.sql != nil {
		selector = epq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if epq.ctx.Unique != nil && *epq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range epq.predicates {
		p(selector)
	}
	for _, p := range epq.order {
		p(selector)
	}
	if offset := epq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := epq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ExamPAGroupBy is the group-by builder for Exam_PA entities.
type ExamPAGroupBy struct {
	selector
	build *ExamPAQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (epgb *ExamPAGroupBy) Aggregate(fns ...AggregateFunc) *ExamPAGroupBy {
	epgb.fns = append(epgb.fns, fns...)
	return epgb
}

// Scan applies the selector query and scans the result into the given value.
func (epgb *ExamPAGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, epgb.build.ctx, "GroupBy")
	if err := epgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamPAQuery, *ExamPAGroupBy](ctx, epgb.build, epgb, epgb.build.inters, v)
}

func (epgb *ExamPAGroupBy) sqlScan(ctx context.Context, root *ExamPAQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(epgb.fns))
	for _, fn := range epgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*epgb.flds)+len(epgb.fns))
		for _, f := range *epgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*epgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := epgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ExamPASelect is the builder for selecting fields of ExamPA entities.
type ExamPASelect struct {
	*ExamPAQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (eps *ExamPASelect) Aggregate(fns ...AggregateFunc) *ExamPASelect {
	eps.fns = append(eps.fns, fns...)
	return eps
}

// Scan applies the selector query and scans the result into the given value.
func (eps *ExamPASelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eps.ctx, "Select")
	if err := eps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamPAQuery, *ExamPASelect](ctx, eps.ExamPAQuery, eps, eps.inters, v)
}

func (eps *ExamPASelect) sqlScan(ctx context.Context, root *ExamPAQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(eps.fns))
	for _, fn := range eps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*eps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
