// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"recruit/ent/circlemaster"
	"recruit/ent/divisionmaster"
	"recruit/ent/exam_applications_ip"
	"recruit/ent/exam_applications_ps"
	"recruit/ent/facility"
	"recruit/ent/predicate"
	"recruit/ent/regionmaster"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FacilityQuery is the builder for querying Facility entities.
type FacilityQuery struct {
	config
	ctx             *QueryContext
	order           []facility.OrderOption
	inters          []Interceptor
	predicates      []predicate.Facility
	withDivisions   *DivisionMasterQuery
	withRegions     *RegionMasterQuery
	withCircles     *CircleMasterQuery
	withCircleRef   *CircleMasterQuery
	withOfficePSRef *ExamApplicationsPSQuery
	withOfficeIPRef *ExamApplicationsIPQuery
	withFKs         bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FacilityQuery builder.
func (fq *FacilityQuery) Where(ps ...predicate.Facility) *FacilityQuery {
	fq.predicates = append(fq.predicates, ps...)
	return fq
}

// Limit the number of records to be returned by this query.
func (fq *FacilityQuery) Limit(limit int) *FacilityQuery {
	fq.ctx.Limit = &limit
	return fq
}

// Offset to start from.
func (fq *FacilityQuery) Offset(offset int) *FacilityQuery {
	fq.ctx.Offset = &offset
	return fq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fq *FacilityQuery) Unique(unique bool) *FacilityQuery {
	fq.ctx.Unique = &unique
	return fq
}

// Order specifies how the records should be ordered.
func (fq *FacilityQuery) Order(o ...facility.OrderOption) *FacilityQuery {
	fq.order = append(fq.order, o...)
	return fq
}

// QueryDivisions chains the current query on the "divisions" edge.
func (fq *FacilityQuery) QueryDivisions() *DivisionMasterQuery {
	query := (&DivisionMasterClient{config: fq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(facility.Table, facility.FieldID, selector),
			sqlgraph.To(divisionmaster.Table, divisionmaster.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, facility.DivisionsTable, facility.DivisionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(fq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRegions chains the current query on the "regions" edge.
func (fq *FacilityQuery) QueryRegions() *RegionMasterQuery {
	query := (&RegionMasterClient{config: fq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(facility.Table, facility.FieldID, selector),
			sqlgraph.To(regionmaster.Table, regionmaster.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, facility.RegionsTable, facility.RegionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(fq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCircles chains the current query on the "circles" edge.
func (fq *FacilityQuery) QueryCircles() *CircleMasterQuery {
	query := (&CircleMasterClient{config: fq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(facility.Table, facility.FieldID, selector),
			sqlgraph.To(circlemaster.Table, circlemaster.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, facility.CirclesTable, facility.CirclesColumn),
		)
		fromU = sqlgraph.SetNeighbors(fq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCircleRef chains the current query on the "circle_ref" edge.
func (fq *FacilityQuery) QueryCircleRef() *CircleMasterQuery {
	query := (&CircleMasterClient{config: fq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(facility.Table, facility.FieldID, selector),
			sqlgraph.To(circlemaster.Table, circlemaster.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, facility.CircleRefTable, facility.CircleRefColumn),
		)
		fromU = sqlgraph.SetNeighbors(fq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOfficePSRef chains the current query on the "Office_PS_Ref" edge.
func (fq *FacilityQuery) QueryOfficePSRef() *ExamApplicationsPSQuery {
	query := (&ExamApplicationsPSClient{config: fq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(facility.Table, facility.FieldID, selector),
			sqlgraph.To(exam_applications_ps.Table, exam_applications_ps.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, facility.OfficePSRefTable, facility.OfficePSRefColumn),
		)
		fromU = sqlgraph.SetNeighbors(fq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOfficeIPRef chains the current query on the "Office_IP_Ref" edge.
func (fq *FacilityQuery) QueryOfficeIPRef() *ExamApplicationsIPQuery {
	query := (&ExamApplicationsIPClient{config: fq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(facility.Table, facility.FieldID, selector),
			sqlgraph.To(exam_applications_ip.Table, exam_applications_ip.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, facility.OfficeIPRefTable, facility.OfficeIPRefColumn),
		)
		fromU = sqlgraph.SetNeighbors(fq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Facility entity from the query.
// Returns a *NotFoundError when no Facility was found.
func (fq *FacilityQuery) First(ctx context.Context) (*Facility, error) {
	nodes, err := fq.Limit(1).All(setContextOp(ctx, fq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{facility.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fq *FacilityQuery) FirstX(ctx context.Context) *Facility {
	node, err := fq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Facility ID from the query.
// Returns a *NotFoundError when no Facility ID was found.
func (fq *FacilityQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = fq.Limit(1).IDs(setContextOp(ctx, fq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{facility.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fq *FacilityQuery) FirstIDX(ctx context.Context) int32 {
	id, err := fq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Facility entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Facility entity is found.
// Returns a *NotFoundError when no Facility entities are found.
func (fq *FacilityQuery) Only(ctx context.Context) (*Facility, error) {
	nodes, err := fq.Limit(2).All(setContextOp(ctx, fq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{facility.Label}
	default:
		return nil, &NotSingularError{facility.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fq *FacilityQuery) OnlyX(ctx context.Context) *Facility {
	node, err := fq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Facility ID in the query.
// Returns a *NotSingularError when more than one Facility ID is found.
// Returns a *NotFoundError when no entities are found.
func (fq *FacilityQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = fq.Limit(2).IDs(setContextOp(ctx, fq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{facility.Label}
	default:
		err = &NotSingularError{facility.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fq *FacilityQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := fq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Facilities.
func (fq *FacilityQuery) All(ctx context.Context) ([]*Facility, error) {
	ctx = setContextOp(ctx, fq.ctx, "All")
	if err := fq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Facility, *FacilityQuery]()
	return withInterceptors[[]*Facility](ctx, fq, qr, fq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fq *FacilityQuery) AllX(ctx context.Context) []*Facility {
	nodes, err := fq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Facility IDs.
func (fq *FacilityQuery) IDs(ctx context.Context) (ids []int32, err error) {
	if fq.ctx.Unique == nil && fq.path != nil {
		fq.Unique(true)
	}
	ctx = setContextOp(ctx, fq.ctx, "IDs")
	if err = fq.Select(facility.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fq *FacilityQuery) IDsX(ctx context.Context) []int32 {
	ids, err := fq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fq *FacilityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fq.ctx, "Count")
	if err := fq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fq, querierCount[*FacilityQuery](), fq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fq *FacilityQuery) CountX(ctx context.Context) int {
	count, err := fq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fq *FacilityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fq.ctx, "Exist")
	switch _, err := fq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fq *FacilityQuery) ExistX(ctx context.Context) bool {
	exist, err := fq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FacilityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fq *FacilityQuery) Clone() *FacilityQuery {
	if fq == nil {
		return nil
	}
	return &FacilityQuery{
		config:          fq.config,
		ctx:             fq.ctx.Clone(),
		order:           append([]facility.OrderOption{}, fq.order...),
		inters:          append([]Interceptor{}, fq.inters...),
		predicates:      append([]predicate.Facility{}, fq.predicates...),
		withDivisions:   fq.withDivisions.Clone(),
		withRegions:     fq.withRegions.Clone(),
		withCircles:     fq.withCircles.Clone(),
		withCircleRef:   fq.withCircleRef.Clone(),
		withOfficePSRef: fq.withOfficePSRef.Clone(),
		withOfficeIPRef: fq.withOfficeIPRef.Clone(),
		// clone intermediate query.
		sql:  fq.sql.Clone(),
		path: fq.path,
	}
}

// WithDivisions tells the query-builder to eager-load the nodes that are connected to
// the "divisions" edge. The optional arguments are used to configure the query builder of the edge.
func (fq *FacilityQuery) WithDivisions(opts ...func(*DivisionMasterQuery)) *FacilityQuery {
	query := (&DivisionMasterClient{config: fq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fq.withDivisions = query
	return fq
}

// WithRegions tells the query-builder to eager-load the nodes that are connected to
// the "regions" edge. The optional arguments are used to configure the query builder of the edge.
func (fq *FacilityQuery) WithRegions(opts ...func(*RegionMasterQuery)) *FacilityQuery {
	query := (&RegionMasterClient{config: fq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fq.withRegions = query
	return fq
}

// WithCircles tells the query-builder to eager-load the nodes that are connected to
// the "circles" edge. The optional arguments are used to configure the query builder of the edge.
func (fq *FacilityQuery) WithCircles(opts ...func(*CircleMasterQuery)) *FacilityQuery {
	query := (&CircleMasterClient{config: fq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fq.withCircles = query
	return fq
}

// WithCircleRef tells the query-builder to eager-load the nodes that are connected to
// the "circle_ref" edge. The optional arguments are used to configure the query builder of the edge.
func (fq *FacilityQuery) WithCircleRef(opts ...func(*CircleMasterQuery)) *FacilityQuery {
	query := (&CircleMasterClient{config: fq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fq.withCircleRef = query
	return fq
}

// WithOfficePSRef tells the query-builder to eager-load the nodes that are connected to
// the "Office_PS_Ref" edge. The optional arguments are used to configure the query builder of the edge.
func (fq *FacilityQuery) WithOfficePSRef(opts ...func(*ExamApplicationsPSQuery)) *FacilityQuery {
	query := (&ExamApplicationsPSClient{config: fq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fq.withOfficePSRef = query
	return fq
}

// WithOfficeIPRef tells the query-builder to eager-load the nodes that are connected to
// the "Office_IP_Ref" edge. The optional arguments are used to configure the query builder of the edge.
func (fq *FacilityQuery) WithOfficeIPRef(opts ...func(*ExamApplicationsIPQuery)) *FacilityQuery {
	query := (&ExamApplicationsIPClient{config: fq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fq.withOfficeIPRef = query
	return fq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		FacilityCode int32 `json:"FacilityCode,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Facility.Query().
//		GroupBy(facility.FieldFacilityCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fq *FacilityQuery) GroupBy(field string, fields ...string) *FacilityGroupBy {
	fq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FacilityGroupBy{build: fq}
	grbuild.flds = &fq.ctx.Fields
	grbuild.label = facility.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		FacilityCode int32 `json:"FacilityCode,omitempty"`
//	}
//
//	client.Facility.Query().
//		Select(facility.FieldFacilityCode).
//		Scan(ctx, &v)
func (fq *FacilityQuery) Select(fields ...string) *FacilitySelect {
	fq.ctx.Fields = append(fq.ctx.Fields, fields...)
	sbuild := &FacilitySelect{FacilityQuery: fq}
	sbuild.label = facility.Label
	sbuild.flds, sbuild.scan = &fq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FacilitySelect configured with the given aggregations.
func (fq *FacilityQuery) Aggregate(fns ...AggregateFunc) *FacilitySelect {
	return fq.Select().Aggregate(fns...)
}

func (fq *FacilityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fq); err != nil {
				return err
			}
		}
	}
	for _, f := range fq.ctx.Fields {
		if !facility.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fq.path != nil {
		prev, err := fq.path(ctx)
		if err != nil {
			return err
		}
		fq.sql = prev
	}
	return nil
}

func (fq *FacilityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Facility, error) {
	var (
		nodes       = []*Facility{}
		withFKs     = fq.withFKs
		_spec       = fq.querySpec()
		loadedTypes = [6]bool{
			fq.withDivisions != nil,
			fq.withRegions != nil,
			fq.withCircles != nil,
			fq.withCircleRef != nil,
			fq.withOfficePSRef != nil,
			fq.withOfficeIPRef != nil,
		}
	)
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, facility.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Facility).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Facility{config: fq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := fq.withDivisions; query != nil {
		if err := fq.loadDivisions(ctx, query, nodes, nil,
			func(n *Facility, e *DivisionMaster) { n.Edges.Divisions = e }); err != nil {
			return nil, err
		}
	}
	if query := fq.withRegions; query != nil {
		if err := fq.loadRegions(ctx, query, nodes, nil,
			func(n *Facility, e *RegionMaster) { n.Edges.Regions = e }); err != nil {
			return nil, err
		}
	}
	if query := fq.withCircles; query != nil {
		if err := fq.loadCircles(ctx, query, nodes, nil,
			func(n *Facility, e *CircleMaster) { n.Edges.Circles = e }); err != nil {
			return nil, err
		}
	}
	if query := fq.withCircleRef; query != nil {
		if err := fq.loadCircleRef(ctx, query, nodes,
			func(n *Facility) { n.Edges.CircleRef = []*CircleMaster{} },
			func(n *Facility, e *CircleMaster) { n.Edges.CircleRef = append(n.Edges.CircleRef, e) }); err != nil {
			return nil, err
		}
	}
	if query := fq.withOfficePSRef; query != nil {
		if err := fq.loadOfficePSRef(ctx, query, nodes,
			func(n *Facility) { n.Edges.OfficePSRef = []*Exam_Applications_PS{} },
			func(n *Facility, e *Exam_Applications_PS) { n.Edges.OfficePSRef = append(n.Edges.OfficePSRef, e) }); err != nil {
			return nil, err
		}
	}
	if query := fq.withOfficeIPRef; query != nil {
		if err := fq.loadOfficeIPRef(ctx, query, nodes,
			func(n *Facility) { n.Edges.OfficeIPRef = []*Exam_Applications_IP{} },
			func(n *Facility, e *Exam_Applications_IP) { n.Edges.OfficeIPRef = append(n.Edges.OfficeIPRef, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (fq *FacilityQuery) loadDivisions(ctx context.Context, query *DivisionMasterQuery, nodes []*Facility, init func(*Facility), assign func(*Facility, *DivisionMaster)) error {
	ids := make([]int32, 0, len(nodes))
	nodeids := make(map[int32][]*Facility)
	for i := range nodes {
		fk := nodes[i].DivisionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(divisionmaster.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "DivisionID" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (fq *FacilityQuery) loadRegions(ctx context.Context, query *RegionMasterQuery, nodes []*Facility, init func(*Facility), assign func(*Facility, *RegionMaster)) error {
	ids := make([]int32, 0, len(nodes))
	nodeids := make(map[int32][]*Facility)
	for i := range nodes {
		fk := nodes[i].RegionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(regionmaster.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "RegionID" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (fq *FacilityQuery) loadCircles(ctx context.Context, query *CircleMasterQuery, nodes []*Facility, init func(*Facility), assign func(*Facility, *CircleMaster)) error {
	ids := make([]int32, 0, len(nodes))
	nodeids := make(map[int32][]*Facility)
	for i := range nodes {
		fk := nodes[i].CircleID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(circlemaster.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "CircleID" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (fq *FacilityQuery) loadCircleRef(ctx context.Context, query *CircleMasterQuery, nodes []*Facility, init func(*Facility), assign func(*Facility, *CircleMaster)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*Facility)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.CircleMaster(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(facility.CircleRefColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.facility_circle_ref
		if fk == nil {
			return fmt.Errorf(`foreign-key "facility_circle_ref" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "facility_circle_ref" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (fq *FacilityQuery) loadOfficePSRef(ctx context.Context, query *ExamApplicationsPSQuery, nodes []*Facility, init func(*Facility), assign func(*Facility, *Exam_Applications_PS)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*Facility)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Exam_Applications_PS(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(facility.OfficePSRefColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.facility_office_ps_ref
		if fk == nil {
			return fmt.Errorf(`foreign-key "facility_office_ps_ref" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "facility_office_ps_ref" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (fq *FacilityQuery) loadOfficeIPRef(ctx context.Context, query *ExamApplicationsIPQuery, nodes []*Facility, init func(*Facility), assign func(*Facility, *Exam_Applications_IP)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*Facility)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Exam_Applications_IP(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(facility.OfficeIPRefColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.facility_office_ip_ref
		if fk == nil {
			return fmt.Errorf(`foreign-key "facility_office_ip_ref" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "facility_office_ip_ref" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (fq *FacilityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fq.querySpec()
	_spec.Node.Columns = fq.ctx.Fields
	if len(fq.ctx.Fields) > 0 {
		_spec.Unique = fq.ctx.Unique != nil && *fq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fq.driver, _spec)
}

func (fq *FacilityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(facility.Table, facility.Columns, sqlgraph.NewFieldSpec(facility.FieldID, field.TypeInt32))
	_spec.From = fq.sql
	if unique := fq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fq.path != nil {
		_spec.Unique = true
	}
	if fields := fq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, facility.FieldID)
		for i := range fields {
			if fields[i] != facility.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if fq.withDivisions != nil {
			_spec.Node.AddColumnOnce(facility.FieldDivisionID)
		}
		if fq.withRegions != nil {
			_spec.Node.AddColumnOnce(facility.FieldRegionID)
		}
		if fq.withCircles != nil {
			_spec.Node.AddColumnOnce(facility.FieldCircleID)
		}
	}
	if ps := fq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fq *FacilityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fq.driver.Dialect())
	t1 := builder.Table(facility.Table)
	columns := fq.ctx.Fields
	if len(columns) == 0 {
		columns = facility.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fq.sql != nil {
		selector = fq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fq.ctx.Unique != nil && *fq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fq.predicates {
		p(selector)
	}
	for _, p := range fq.order {
		p(selector)
	}
	if offset := fq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FacilityGroupBy is the group-by builder for Facility entities.
type FacilityGroupBy struct {
	selector
	build *FacilityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fgb *FacilityGroupBy) Aggregate(fns ...AggregateFunc) *FacilityGroupBy {
	fgb.fns = append(fgb.fns, fns...)
	return fgb
}

// Scan applies the selector query and scans the result into the given value.
func (fgb *FacilityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fgb.build.ctx, "GroupBy")
	if err := fgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FacilityQuery, *FacilityGroupBy](ctx, fgb.build, fgb, fgb.build.inters, v)
}

func (fgb *FacilityGroupBy) sqlScan(ctx context.Context, root *FacilityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fgb.fns))
	for _, fn := range fgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fgb.flds)+len(fgb.fns))
		for _, f := range *fgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FacilitySelect is the builder for selecting fields of Facility entities.
type FacilitySelect struct {
	*FacilityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fs *FacilitySelect) Aggregate(fns ...AggregateFunc) *FacilitySelect {
	fs.fns = append(fs.fns, fns...)
	return fs
}

// Scan applies the selector query and scans the result into the given value.
func (fs *FacilitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fs.ctx, "Select")
	if err := fs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FacilityQuery, *FacilitySelect](ctx, fs.FacilityQuery, fs, fs.inters, v)
}

func (fs *FacilitySelect) sqlScan(ctx context.Context, root *FacilityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fs.fns))
	for _, fn := range fs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
