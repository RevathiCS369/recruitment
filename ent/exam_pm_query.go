// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"recruit/ent/exam_pm"
	"recruit/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ExamPMQuery is the builder for querying Exam_PM entities.
type ExamPMQuery struct {
	config
	ctx        *QueryContext
	order      []exam_pm.OrderOption
	inters     []Interceptor
	predicates []predicate.Exam_PM
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ExamPMQuery builder.
func (epq *ExamPMQuery) Where(ps ...predicate.Exam_PM) *ExamPMQuery {
	epq.predicates = append(epq.predicates, ps...)
	return epq
}

// Limit the number of records to be returned by this query.
func (epq *ExamPMQuery) Limit(limit int) *ExamPMQuery {
	epq.ctx.Limit = &limit
	return epq
}

// Offset to start from.
func (epq *ExamPMQuery) Offset(offset int) *ExamPMQuery {
	epq.ctx.Offset = &offset
	return epq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (epq *ExamPMQuery) Unique(unique bool) *ExamPMQuery {
	epq.ctx.Unique = &unique
	return epq
}

// Order specifies how the records should be ordered.
func (epq *ExamPMQuery) Order(o ...exam_pm.OrderOption) *ExamPMQuery {
	epq.order = append(epq.order, o...)
	return epq
}

// First returns the first Exam_PM entity from the query.
// Returns a *NotFoundError when no Exam_PM was found.
func (epq *ExamPMQuery) First(ctx context.Context) (*Exam_PM, error) {
	nodes, err := epq.Limit(1).All(setContextOp(ctx, epq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{exam_pm.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (epq *ExamPMQuery) FirstX(ctx context.Context) *Exam_PM {
	node, err := epq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Exam_PM ID from the query.
// Returns a *NotFoundError when no Exam_PM ID was found.
func (epq *ExamPMQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = epq.Limit(1).IDs(setContextOp(ctx, epq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{exam_pm.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (epq *ExamPMQuery) FirstIDX(ctx context.Context) int32 {
	id, err := epq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Exam_PM entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Exam_PM entity is found.
// Returns a *NotFoundError when no Exam_PM entities are found.
func (epq *ExamPMQuery) Only(ctx context.Context) (*Exam_PM, error) {
	nodes, err := epq.Limit(2).All(setContextOp(ctx, epq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{exam_pm.Label}
	default:
		return nil, &NotSingularError{exam_pm.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (epq *ExamPMQuery) OnlyX(ctx context.Context) *Exam_PM {
	node, err := epq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Exam_PM ID in the query.
// Returns a *NotSingularError when more than one Exam_PM ID is found.
// Returns a *NotFoundError when no entities are found.
func (epq *ExamPMQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = epq.Limit(2).IDs(setContextOp(ctx, epq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{exam_pm.Label}
	default:
		err = &NotSingularError{exam_pm.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (epq *ExamPMQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := epq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Exam_PMs.
func (epq *ExamPMQuery) All(ctx context.Context) ([]*Exam_PM, error) {
	ctx = setContextOp(ctx, epq.ctx, "All")
	if err := epq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Exam_PM, *ExamPMQuery]()
	return withInterceptors[[]*Exam_PM](ctx, epq, qr, epq.inters)
}

// AllX is like All, but panics if an error occurs.
func (epq *ExamPMQuery) AllX(ctx context.Context) []*Exam_PM {
	nodes, err := epq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Exam_PM IDs.
func (epq *ExamPMQuery) IDs(ctx context.Context) (ids []int32, err error) {
	if epq.ctx.Unique == nil && epq.path != nil {
		epq.Unique(true)
	}
	ctx = setContextOp(ctx, epq.ctx, "IDs")
	if err = epq.Select(exam_pm.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (epq *ExamPMQuery) IDsX(ctx context.Context) []int32 {
	ids, err := epq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (epq *ExamPMQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, epq.ctx, "Count")
	if err := epq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, epq, querierCount[*ExamPMQuery](), epq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (epq *ExamPMQuery) CountX(ctx context.Context) int {
	count, err := epq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (epq *ExamPMQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, epq.ctx, "Exist")
	switch _, err := epq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (epq *ExamPMQuery) ExistX(ctx context.Context) bool {
	exist, err := epq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ExamPMQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (epq *ExamPMQuery) Clone() *ExamPMQuery {
	if epq == nil {
		return nil
	}
	return &ExamPMQuery{
		config:     epq.config,
		ctx:        epq.ctx.Clone(),
		order:      append([]exam_pm.OrderOption{}, epq.order...),
		inters:     append([]Interceptor{}, epq.inters...),
		predicates: append([]predicate.Exam_PM{}, epq.predicates...),
		// clone intermediate query.
		sql:  epq.sql.Clone(),
		path: epq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ExamNameCode string `json:"ExamNameCode,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ExamPM.Query().
//		GroupBy(exam_pm.FieldExamNameCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (epq *ExamPMQuery) GroupBy(field string, fields ...string) *ExamPMGroupBy {
	epq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ExamPMGroupBy{build: epq}
	grbuild.flds = &epq.ctx.Fields
	grbuild.label = exam_pm.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ExamNameCode string `json:"ExamNameCode,omitempty"`
//	}
//
//	client.ExamPM.Query().
//		Select(exam_pm.FieldExamNameCode).
//		Scan(ctx, &v)
func (epq *ExamPMQuery) Select(fields ...string) *ExamPMSelect {
	epq.ctx.Fields = append(epq.ctx.Fields, fields...)
	sbuild := &ExamPMSelect{ExamPMQuery: epq}
	sbuild.label = exam_pm.Label
	sbuild.flds, sbuild.scan = &epq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ExamPMSelect configured with the given aggregations.
func (epq *ExamPMQuery) Aggregate(fns ...AggregateFunc) *ExamPMSelect {
	return epq.Select().Aggregate(fns...)
}

func (epq *ExamPMQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range epq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, epq); err != nil {
				return err
			}
		}
	}
	for _, f := range epq.ctx.Fields {
		if !exam_pm.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if epq.path != nil {
		prev, err := epq.path(ctx)
		if err != nil {
			return err
		}
		epq.sql = prev
	}
	return nil
}

func (epq *ExamPMQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Exam_PM, error) {
	var (
		nodes = []*Exam_PM{}
		_spec = epq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Exam_PM).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Exam_PM{config: epq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, epq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (epq *ExamPMQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := epq.querySpec()
	_spec.Node.Columns = epq.ctx.Fields
	if len(epq.ctx.Fields) > 0 {
		_spec.Unique = epq.ctx.Unique != nil && *epq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, epq.driver, _spec)
}

func (epq *ExamPMQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(exam_pm.Table, exam_pm.Columns, sqlgraph.NewFieldSpec(exam_pm.FieldID, field.TypeInt32))
	_spec.From = epq.sql
	if unique := epq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if epq.path != nil {
		_spec.Unique = true
	}
	if fields := epq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, exam_pm.FieldID)
		for i := range fields {
			if fields[i] != exam_pm.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := epq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := epq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := epq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := epq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (epq *ExamPMQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(epq.driver.Dialect())
	t1 := builder.Table(exam_pm.Table)
	columns := epq.ctx.Fields
	if len(columns) == 0 {
		columns = exam_pm.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if epq.sql != nil {
		selector = epq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if epq.ctx.Unique != nil && *epq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range epq.predicates {
		p(selector)
	}
	for _, p := range epq.order {
		p(selector)
	}
	if offset := epq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := epq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ExamPMGroupBy is the group-by builder for Exam_PM entities.
type ExamPMGroupBy struct {
	selector
	build *ExamPMQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (epgb *ExamPMGroupBy) Aggregate(fns ...AggregateFunc) *ExamPMGroupBy {
	epgb.fns = append(epgb.fns, fns...)
	return epgb
}

// Scan applies the selector query and scans the result into the given value.
func (epgb *ExamPMGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, epgb.build.ctx, "GroupBy")
	if err := epgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamPMQuery, *ExamPMGroupBy](ctx, epgb.build, epgb, epgb.build.inters, v)
}

func (epgb *ExamPMGroupBy) sqlScan(ctx context.Context, root *ExamPMQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(epgb.fns))
	for _, fn := range epgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*epgb.flds)+len(epgb.fns))
		for _, f := range *epgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*epgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := epgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ExamPMSelect is the builder for selecting fields of ExamPM entities.
type ExamPMSelect struct {
	*ExamPMQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (eps *ExamPMSelect) Aggregate(fns ...AggregateFunc) *ExamPMSelect {
	eps.fns = append(eps.fns, fns...)
	return eps
}

// Scan applies the selector query and scans the result into the given value.
func (eps *ExamPMSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eps.ctx, "Select")
	if err := eps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamPMQuery, *ExamPMSelect](ctx, eps.ExamPMQuery, eps, eps.inters, v)
}

func (eps *ExamPMSelect) sqlScan(ctx context.Context, root *ExamPMQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(eps.fns))
	for _, fn := range eps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*eps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
